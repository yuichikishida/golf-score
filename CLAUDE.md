# ゴルフスコア記録・分析アプリ

## プロジェクト概要

ゴルフのスコアを入力してGoogleドライブに貯め、あとからClaudeで分析するためのWebアプリ。
**アプリは入力に徹し、知能は後段(Claude)に置く**というのが設計の中核。
議事録自動化プロジェクト(gijiroku)と同じ構図で、録音アプリにあたるのがこのアプリ。

## アーキテクチャ

```
[ラウンド中]              [保存]                    [あとで]
index.html (PWA)          Googleドライブ            Claudeに投げる
スコア/パット/FW/OB  ──→   JSON + Markdown    ──→   分析・練習メニュー
localStorageに逐次保存    (端末内にも残る)
```

- サーバーなし、APIキーなし、外部送信なし。GitHub Pagesの静的ホスティングのみ
- データは localStorage が正。エクスポートは明示的な操作(自動同期はしない)
- 保存経路は3段: File System Access API(PC Chrome→ドライブ同期フォルダに直接書き込み)
  → Web Share API(iPhone→共有シートからドライブ)→ ダウンロード

## ファイル構成

- `index.html` — アプリ本体。HTML/CSS/JSを1ファイルに収める(ビルド工程を入れない)
  - 画面: ホーム / 新規ラウンド / 入力 / サマリー / コース管理 / コース編集
  - localStorageキー: `golf.courses` `golf.rounds` `golf.current`
  - 保存先フォルダのハンドルは IndexedDB (`golf-score` / `kv` / `dir`)
- `sw.js` — Service Worker。ネットワーク優先・失敗時キャッシュ。ゴルフ場の電波対策
- `manifest.webmanifest` / `icon.svg` — ホーム画面追加用
- `コース/` — コースデータのJSONと書式の説明
- `分析プロンプト.md` — 分析観点の原文。**分析を改善したいときはここを直す。アプリは触らない**
- `要件定義.md` — 設計の経緯と、検討して捨てた案

## データモデル

ホールごとに記録するのは5項目のみ: `strokes` `putts` `fairway` `ob` `penalty`(+ `note`)。
これで パーオン率 / FWキープ率 / スクランブリング率 / 平均パット / 罰打 / スコア分布 が出る。

- パーオン判定: `strokes - putts <= par - 2`
- FWキープはパー4以上のホールのみ対象
- スクランブリング: パーオンを外したホールのうち、パー以上で上がれた割合

## 規約

- UIテキスト・コメント・ドキュメントは日本語
- `index.html` は単一ファイル構成を維持する(ビルド工程を入れない)
- **入力項目を増やすときは慎重に。** ラウンド中の操作が増えると入力されなくなり、
  結果としてデータの質が下がる。1ホール3タップを基準にする
- **推測でデータを埋めない。** 未入力は未入力のまま残す(集計側で除外する)。
  誤ったデータは、欠けたデータより分析を悪くする
- 分析ロジックをアプリに実装しない。プロンプト側に置く

## Claudeへの依頼でよくあるもの

- **コースJSONの作成**: 「〇〇CCのスコアカードをJSONにして」。書式は `コース/README.md`。
  Web検索の情報は誤りが混じるので、**スコアカードの写真をもらえればそれを最優先で使う**。
  推測でヤーデージを埋めず、分からない項目は省く
- **ラウンドの分析**: `分析プロンプト.md` の観点で。記録が5項目しかないことを踏まえ、
  データから言えないことは言えないと明示する

## 開発

```sh
npx http-server . -p 8811    # ローカル確認
```

デプロイはGitHub Pages(mainブランチ、root)。mainにプッシュすれば数分で反映。
HTTPSでないとService Workerが動かない(localhostは例外)。
