# 要件定義と設計の経緯

## 1. 出発点

きっかけは Golfshot のようなGPS連動のゴルフアプリ。作りたい理由は次の2点だった。

1. **ラウンド後の復習と分析ができるようにしたい**(こちらが主目的)
2. **Apple Watchの電池がハーフで切れる**のを解消したい

GPSによる残り距離表示は「副産物であればいい」程度の位置づけ。

## 2. 最終的な決定

| 項目 | 決定 |
| --- | --- |
| プラットフォーム | **Webアプリ**。GitHub Pagesで無料公開。iPhone/PC/Androidで動く |
| 記録の粒度 | ホールごとに **打数・パット数・ティーショット方向・OB・その他の罰打**(1ホール3タップ) |
| データの置き場所 | 端末内(localStorage)が正。ラウンド後にGoogleドライブへ書き出す |
| 分析 | **アプリでは行わない。**書き出したファイルをClaudeに渡して分析させる |
| コースデータ | 事前にClaudeにスコアカードを調べさせ(または写真から)JSON化してアプリに取り込む |
| GPS・距離表示 | **作らない** |
| Apple Watch | 専用アプリは作らない。将来「ショートカット」での代用を検証する |

## 3. なぜこうなったか

### 3-1. 有料アプリを作らない、という前提

当初はネイティブのiOS + watchOSアプリを検討した。Apple Watchでのスコア入力を実現するには
ネイティブ実装が必須で、それには **Apple Developer Program 年$99** が要る。
無料アカウントではアプリが7日で失効し、ラウンドのたびに再インストールが必要になって実用にならない。

「有料アプリを作るつもりはない」という前提が確定した時点で、ネイティブは選択肢から外れた。
Webアプリなら費用はゼロで、iPhoneでもPCでも動き、インストールも不要。

副次的な利点として、**Webアプリなら開発側(Claude)がその場でビルドして動作確認できる**。
ネイティブだとXcodeが必要で、コードは書けても検証は人間の手に委ねるしかなかった。

### 3-2. GPSと距離表示をやめた理由

これを捨てたことで、当初の課題2つが両方とも自動的に解決した。

- **バッテリー問題が消滅した。** Watchの電池が切れる原因はGPSの常時稼働だった。
  GPSを使わないアプリは電池を食わない。スマホのポケット内での消費もほぼゼロ
- **最大の技術的難所が消滅した。** 距離を出すにはホールごとのグリーン座標が必要で、
  そのデータをどう入手するかが最も難しい問題だった(§4に調査結果)

そして、当初から距離表示は「副産物であればいい」程度の位置づけだった。
主目的である分析には、座標は要らない。

### 3-3. 分析をアプリに実装しない理由

分析をアプリの外(Claude)に置くと、次の利点がある。

- **分析の内容を後からいくらでも変えられる。**「パットの分析が浅い」と思ったら
  プロンプトを書き換えるだけでよく、アプリの更新もデプロイも要らない
- **アプリにAPIキーを持たせなくて済む。** キー管理も課金も発生しない
- **アプリが単純になり、壊れなくなる。** 入力と保存しかしていないので、
  ラウンド中に不具合で使えなくなるリスクが低い

これは議事録自動化プロジェクト(gijiroku)と同じ構図。
録音アプリは録音に徹し、議事録の生成は後段に置いている。

### 3-4. 記録項目を5つに絞った理由

当初はショット1打ごとに「位置・クラブ・ライ」を記録して **Strokes Gained**
(ティーショット/アプローチ/グリーン周り/パットのどこで何打損したかの数値化)まで
出すことを検討した。分析としては最も価値が高い。

ただしそれは1打ごとに操作が要る。ラウンド中の操作が増えると、結局入力されなくなり、
**欠けたデータで分析することになって本末転倒**になる。

打数・パット数・FW・OB・罰打の5つに絞ると、1ホール3タップで済み、
それでいて次のものが出せる。

- パーオン率(GIR) — `打数 − パット数 ≦ パー − 2`
- フェアウェイキープ率(左右どちらに外したかも)
- スクランブリング率 — パーオンを外したホールでパー以上で上がれた率
- 平均パット・3パット数・パーオンしたホールの平均パット
- 罰打の合計、スコア分布

つまり「**ショットとパットのどちらで損しているか**」は十分に分かる。
まずこの粒度で何ラウンドか回し、物足りなければ項目を足す。

コースデータに **HDCP順位** を入れておけば、
「難しいホールで崩れているのか、簡単なホールを取りこぼしているのか」まで踏み込める。
後者のほうが改善余地が大きいので、この判定は効く。

## 4. コースデータの調査結果(2026-08)

距離表示を作る場合、ホールごとのグリーン座標が要る。その入手方法を調べた結果。

| 提供元 | 内容 | 判定 |
| --- | --- | --- |
| [iGolf (L1)](https://igolf.com/solutions/golf-course-data/) | 業界標準。グリーン手前/中央/奥、ティー中央の座標。多くの市販アプリの裏側 | 法人向けライセンスで自己申込み不可・高額 |
| [Golf Intelligence](https://golfintelligence.com/) | 全世界の全ホール、GPS + グリーン傾斜。無料お試し200クレジット、以降有料 | 日本のコース収録が不明 |
| [golfapi.io](https://golfapi.io/) | グリーン・ティーの座標、POI | 有料・日本収録が不明 |
| [GolfCourseAPI](https://golfcourseapi.com/) | 無料・約30,000コース。ただし持っているのは**コース名・所在地・ティー情報・レーティング**のみ | **ホール座標が無く距離計測に使えない**。所在地はクラブ単位の1点 |
| [OpenStreetMap](https://wiki.openstreetmap.org/wiki/Tag:golf=hole) + Overpass | `golf=hole`(ティー→グリーンの中心線)、`golf=green`。無料・キー不要 | 必要な形状は揃うが、収録は有志次第で日本は網羅的でない |

**結論: 無料で日本のコースを網羅している座標データは存在しない。**
距離表示をやるなら、有料APIを試すか、自分で衛星写真上にタップ登録するしかなかった。
GPSごと捨てたことでこの問題自体が不要になった。

なお、**パー・HDCP・ヤーデージ(スコアカードの情報)** は座標と違い、
コース公式サイトのコースガイドに載っている。これはClaudeに調べさせるか、
スコアカードの写真から起こせばよい。座標ほどの困難はない。

## 5. 捨てた案

| 案 | 捨てた理由 |
| --- | --- |
| ネイティブ iOS + watchOS アプリ | 年$99が必要。有料アプリを作らない前提と合わない |
| GPS距離表示 | バッテリーを食い、コース座標の入手が困難。主目的でもない |
| ショット単位の記録 + Strokes Gained | ラウンド中の操作が増えすぎ、入力されなくなる懸念 |
| アプリ内でのAI分析(Gemini API等) | APIキー管理が発生し、分析の改善にアプリ更新が必要になる |
| GolfCourseAPI等の外部コースDB連携 | 座標が無い、または日本の収録が不明。手元にJSONを持つほうが確実 |

## 6. 未着手・今後

- 同伴者のスコア入力
- ハンディキャップの算出(JGA/USGA方式)
- **Apple Watchからの入力をショートカットで代用できないかの検証。**
  watchOSのショートカットは対応アクションが限られるため、動くかどうかは試すまで分からない
- 蓄積したラウンドを横断した傾向分析(現状は1ラウンドずつClaudeに渡す運用)
