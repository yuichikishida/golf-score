# ゴルフ スコア記録・分析アプリ 要件定義

## 1. 目的

**主目的は「ラウンド後の復習と分析」。** GPS距離表示は副産物として位置づける。
既存アプリ(Golfshot等)で困っている 2 点を解消することが成功条件。

| 課題 | 本アプリでの解決方針 |
| --- | --- |
| Apple Watch の電池がハーフ(9ホール)で切れる | GPS を「常時計測」から「必要な瞬間だけ」に変える。詳細は §4 |
| スコアを見返しても改善点が分からない | ショット 1 打ごとに「位置・クラブ・ライ」を記録し、Strokes Gained 等で弱点を数値化。詳細は §6 |

## 2. 決定事項(2026-08-30 ヒアリング)

| 項目 | 決定 |
| --- | --- |
| 記録の粒度 | **ショット位置まで記録**(打つ前に Watch でクラブを選んでタップ) |
| Watch の役割 | **Watch 単体でもラウンドを完結できる**(iPhone をカートに置いても動く) |
| コースデータ | 外部 DB を調査 → 結論は §7 |
| 利用範囲 | **自分 + ゴルフ仲間数人**(TestFlight 配布、最大 100 人) |

## 3. 結論:作れる

技術的な障壁はない。ただし前提条件が 3 つある。

1. **ネイティブ実装が必須。** Apple Watch アプリは Web では作れないため Swift / SwiftUI + watchOS。
   既存の `index.html` のような「単一ファイル・ビルド不要」の方針はこのアプリには適用できない。
2. **Apple Developer Program($99/年)が必要。** 無料アカウントではアプリが 7 日で失効し、
   ラウンドのたびに再インストールが必要になるため実用にならない。仲間への TestFlight 配布にも必要。
   **これが唯一の恒常コスト**(サーバー費用はゼロ。データは端末内 + 各自の iCloud)。
3. **ビルドと実機テストは M1 MacBook 上の Xcode で行う。** Claude 側の実行環境は Linux で Xcode がないため、
   ソースコードは書けるがコンパイル・動作確認はできない。特に GPS とバッテリーの挙動は
   シミュレータでは検証不能で、**実際のラウンドで測るしかない**。
   → 対策として、アプリ自体にバッテリー残量ログ機能を最初から組み込む(§4-9)。

## 4. バッテリー設計(最重要)

### 4-1. なぜ既存アプリは電池が切れるのか

1. GPS を 1 秒間隔・最高精度で回しっぱなしにしている(残り距離をリアルタイム更新するため)
2. 常時表示(Always-On)で、暗くなった画面でもアプリが数値を更新し続けている
3. 地図描画と iPhone との頻繁な同期

4.5〜5 時間のラウンドに対して、GPS 常時稼働のワークアウトは公称 5〜7 時間。
そこに画面と通信が乗るため、9〜13 ホールで力尽きる。

### 4-2. 対策(効果の大きい順)

| # | 対策 | 内容 |
| --- | --- | --- |
| 1 | **オンデマンド測位** | GPS を回すのは「距離ボタンを押した時」「ショットを記録した時」だけ。それ以外は完全停止。1 ホールあたり数回・各 10 秒程度で済み、常時稼働比で約 1/10 |
| 2 | **iPhone への日和見オフロード** | iPhone が Bluetooth 圏内にある間は、測位を iPhone 側にやらせて結果だけ Watch に返す(WatchConnectivity の `sendMessage`)。圏外なら Watch 単体の GPS に自動フォールバック。カートに置いても、持ち歩いても動く |
| 3 | **手首を上げた瞬間に先読み測位** | 一発測位は測位開始から 5〜20 秒かかる。アプリがフォアグラウンドになった時点で測位を始め、ユーザーが数値を見る頃には確定している。見終わったら即停止 |
| 4 | **常時表示画面は「更新しない」** | Always-On 時はホール番号とスコアだけの静止画面にし、タイマー・アニメーション・距離更新を一切止める。アプリ内に「省電力モード」トグルも用意 |
| 5 | **Watch に地図を出さない** | 地図描画は高コスト。Watch は大きな数字だけ。地図での振り返りは iPhone 側 |
| 6 | **通信をラウンド終了まで溜める** | ショットごとに同期せず、Watch 内に保存して `transferUserInfo` で日和見送信、または終了時に一括転送 |
| 7 | **ショット記録は 1 点のみ** | 軌跡の連続追跡はしない。1 ラウンド約 90 点の測位で十分。分析にも軌跡は不要 |

### 4-3. アプリを落とさないための仕組み

`HKWorkoutSession`(`HKWorkoutActivityType.golf`)を開始する。これはバックグラウンド実行権を得るためで、
**位置情報の連続収集(`HKWorkoutRouteBuilder`)は使わない**。副次的に、ラウンドが
Apple のフィットネス記録に「ゴルフ」として残る。

### 4-4. 計測を組み込む

`WKInterfaceDevice.batteryLevel` を数分おきにラウンドデータへ記録する。
「何ホール目で何 % だったか」が毎ラウンド残るので、設計の効果を推測ではなく実測で確認し、
足りなければ §4-2 の各対策を段階的に強める。

## 5. アーキテクチャ

```
[Apple Watch]                        [iPhone]                    [iCloud]
ラウンド中の主役                      振り返り・分析の主役          バックアップ/同期
・ホール移動                          ・ショット軌跡を地図表示
・ショット記録(クラブ+位置)   ──→   ・Strokes Gained 等の分析   ──→  CloudKit
・スコア/パット入力                   ・コースデータの取得/編集         (プライベートDB)
・残り距離表示(オンデマンド)         ・仲間との共有
・省電力な静止 Always-On 画面
      │                                    ↑
      └── 圏内なら測位を委譲 ──────────────┘
          (圏外なら Watch 単体GPS)
```

- 言語/UI: Swift + SwiftUI(iOS / watchOS 共通)
- 永続化: SwiftData(iPhone を正とする)+ CloudKit で自分の端末間同期
- Watch → iPhone: WatchConnectivity(`sendMessage` は距離問い合わせ、`transferUserInfo` はデータ送出)
- サーバーなし。音声・位置ともに外部送信しない

## 6. データモデルと分析

### 6-1. 記録するもの

- **Round**: 日付 / コース / 使用ティー / 天候・風 / 同伴者 / メモ
- **Hole**: 番号 / パー / ヤーデージ / ハンディキャップ順位
- **Shot**: 何打目 / **クラブ** / **ライ**(ティー・フェアウェイ・ラフ・バンカー・グリーン・トラブル・ペナルティ)/
  **打った位置の座標** / OB・池などの結果フラグ
- **Putt**: グリーン上の残り距離。**GPS 誤差(3〜5m)はパット距離より大きいため座標では測れない。**
  1m / 2m / 3m / 5m / 7m / 10m / 15m+ のボタンで手入力する
- **BatteryLog**: 時刻と残量(§4-4)

ショットの「終点」は次のショットの始点として自動的に決まるので、入力は 1 打あたり
「クラブを選んでタップ」の 2 操作で済む。

### 6-2. 分析でわかること

| 指標 | 何が分かるか |
| --- | --- |
| **Strokes Gained**(ティーショット / アプローチ / グリーン周り / パット) | 4 分野のどこで何打損しているか。**練習の優先順位がそのまま出る** |
| クラブ別の実飛距離分布(中央値と 8 割レンジ) | 「7I は 140y のつもりが実は 128y」が判明。クラブ選択が変わる |
| 残り距離帯別のアプローチ精度 | 「100〜125y が壊滅的」等、苦手な距離帯の特定 |
| FW キープ率 / パーオン率 / 平均パット / サンドセーブ率 / スクランブリング率 | 一般的な指標。他アプリとの比較用 |
| ホール別・コース別の叩き傾向 | 「このコースの 7 番でいつも 2 打損している」 |
| ラウンド地図リプレイ | 全ショットを衛星写真上に描画して 1 ホールずつ振り返る |

**Strokes Gained のベースラインは 2 系統を用意する。**

1. **自分の過去データから作る個人ベースライン** — 「今日の自分 vs いつもの自分」。
   復習が目的ならこちらの方が有用で、外部データの権利問題も生じない。ラウンドが溜まるほど精度が上がる。
2. 一般的な期待打数(スクラッチ / 90 台 / 100 台)を近似した関数 — 絶対的な立ち位置の把握用。初回から使える。

### 6-3. 既存資産との連携(任意)

このリポジトリには音声の文字起こし基盤(`gijiroku_whisper.py` / mlx-whisper)が既にある。
ホールごとに Watch から音声メモを残し、ラウンド後に Mac でまとめて文字起こしすれば、
「なぜその選択をしたか」という数値に残らない情報を振り返りに載せられる。第 2 フェーズ以降の候補。

## 7. コースデータ:調査結果と結論

残り距離を出すには、ホールごとに最低「グリーン中央の座標」、できれば「ティー位置」「グリーン手前・奥」が要る。

### 7-1. 調査した選択肢

| 提供元 | 内容 | 判定 |
| --- | --- | --- |
| [iGolf (L1)](https://igolf.com/solutions/golf-course-data/) | 業界標準。グリーン手前/中央/奥、ティー中央、ホールあたり最大 4 POI。多くの市販アプリの裏側 | **不採用**。法人向けライセンスで自己申込み不可・高額 |
| [Golf Intelligence](https://golfintelligence.com/) | 全世界の全ホール、GPS + グリーン傾斜。無料お試し 200 クレジット、以降有料 | **保留**。日本のコース収録状況が不明。必要なら問い合わせる価値あり |
| [golfapi.io](https://golfapi.io/) | グリーン・ティーの座標、POI、GPS でのコース検索 | **保留**。有料・日本収録状況が不明 |
| [GolfCourseAPI](https://golfcourseapi.com/) | 無料・約 30,000 コース。ただし公開されている項目は**コース名・所在地・ティーボックス情報・コースレーティング/スロープ** | **距離計測には使えない**。所在地はクラブ単位の 1 点で、ホールごとの座標がない。スコアカード(パー・ヤーデージ)の取り込みには有用 |
| [OpenStreetMap](https://wiki.openstreetmap.org/wiki/Tag:golf=hole) + Overpass API | `golf=hole`(ティー→グリーンの中心線)、`golf=green`(グリーン形状)、バンカー・池・フェアウェイ。無料・API キー不要 | **採用**。必要な形状がそのまま揃う。収録は有志次第で日本は網羅的でない |

### 7-2. 結論:OSM を主軸に、欠けていたら自分で埋める

理由:

- 無料で、API キーもレート制限もベンダー依存もない(このプロジェクトの「無料運用最優先」方針に合う)
- `golf=hole` のウェイはティーからグリーン中央への線そのもので、必要なデータ形式が一致する
- 有料 API は日本のコース収録が確認できず、確認するだけでコストがかかる

日本のコースは収録が薄い可能性が高いので、**必ずフォールバックを用意する**:

1. アプリ内に衛星写真ベースのコース登録画面を持つ。グリーン中央とティーをタップしていくだけ。
   18 ホール × 数点 = 10 分程度。一度作れば以後ずっと使える
2. ラウンド中の自動蓄積 — 「グリーンに乗った」時の位置を記録し、回るほどコースデータが育つ
3. 既存の [FairwayMapper](https://www.fairwaymapper.com/)(ブラウザ上の OSM ゴルフコース専用エディタ)で
   OSM 側に登録してしまえば、他のゴルファーにも還元される

権利について: OSM のデータは ODbL(継承条件あり)。自分と仲間内での利用では問題にならないが、
将来 App Store で一般公開する場合は帰属表示と派生データベースの扱いに注意が必要。

## 8. 開発フェーズ

| フェーズ | 内容 | 完了条件 |
| --- | --- | --- |
| **1** | iPhone アプリの土台。データモデル、ラウンド作成、ホール/ショット入力、コース登録画面(手動タップ) | iPhone だけで 1 ラウンド記録しきれる |
| **2** | Watch アプリ。ショット記録・スコア入力・省電力 GPS・バッテリーログ | Watch だけで 18 ホール回れて電池が残る(**実ラウンドで検証**) |
| **3** | 分析画面。クラブ別飛距離、各種率、地図リプレイ、個人ベースラインでの Strokes Gained | 「次に何を練習すべきか」が画面から読み取れる |
| **4** | OSM 連携でのコース自動取得、TestFlight 配布、同伴者スコア入力 | 仲間が自分の端末で使える |
| **5** | 任意: 音声メモ + 文字起こし連携、一般ベースライン、自動ショット検出 | — |

各フェーズ末で実際にラウンドして使うことを前提にする。特にフェーズ 2 は実測が唯一の検証手段。

## 9. 未決事項

- **リポジトリの置き場所。** 現在は議事録自動化プロジェクトの Public リポジトリ内にあり、
  内容が無関係。専用リポジトリへの分離を推奨する
- 対応 OS バージョンの下限(iOS 17+ / watchOS 10+ を想定)
- 同伴者のスコアをどこまで入力するか(自分だけ / 全員の総打数 / 全員のショット詳細)
- ハンディキャップ算出(JGA/USGA 方式)の要否
